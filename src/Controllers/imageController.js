// const image = require("../Models/image");

// const index = (req, res) => {
//   image.getImages((err, result) => {
//     if (err) {
//       return res.status(500).json({ error: err.message });
//     }
//     if (result.length === 0) {
//       return res.status(404).json({ message: "image kosong" });
//     }
//     res.status(200).json(result);
//   });
// };

// module.exports = { index };
