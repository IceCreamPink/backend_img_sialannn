// const koneksi = require("../Midlleware/Koneksi");

// const getImages = (await, callback) => {
//   const q = `select * from images where deleted_at is null`;
//   koneksi.query(await, q, callback);
// };
// // const getImages = koneksi.query(
// //   `select * from images where deleted_at is null`
// // );

// // async function getNote() {
// //   const [getall] = await koneksi.query(
// //     `select * from images where deleted_at is null`
// //   );
// //   return getall;
// // }

// // const getImages = await getNote();

// // koneksi.query(`select * from images where deleted_at is null`, (err, result)=>{

// // })
// // const getImages = () => {
// //   koneksi.query(
// //     `select * from images where deleted_at is null`,
// //     (err, result) => {
// //       if (err) return console.log(err);
// //       console.log("record updated");
// //     }
// //   );
// // };

// module.exports = { getImages };
